# rh_serv_pkg

This is just a simple controller using service-client to plan the path of the 2D simulated robot.

## Example usage

## Running tests/demos
 run this as: 
`rosrun rh_serv_pkg rh_path_service`

Alternatively, run the corresponding client node with:

`rosrun rh_serv_pkg rh_path_client`
    
